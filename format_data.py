def format_data_for_display(people):
    """The data represents a list of 
    people, each with a given name, 
    family name, and job title. 
    The function should output a 
    list of strings that include 
    each person’s full name (their 
    given_name followed by their 
    family_name),a colon, and their 
    title"""
    pass

def format_data_for_excel(people):
    """nother function to transform 
    the data into comma-separated 
    values for use in Excel"""
    pass